<div class ="container">
	<div class="col-lg-12 text-center">
	  <h1>Editar organização</h1>
	</div>
	<div class= "alert alert-danger col-lg-offset-4 col-lg-4 centralize errorMessageJS">
		<p id="errorMessage">Erro encontrado, verifique os campos em vermelho.</p>
	</div>
  <%= form_for(@ngo,:html=>{:id=>"form-ngo"}) do |f| %>
  <% if @ngo.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@ngo.errors.count, "error") %> prohibited
        this interest from being saved:
      </h2>
      <ul>
        <% @ngo.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <%= render 'form', f:f %>
  <div class="row">
    <div class="col-lg-offset-4 col-lg-4 margin-end-top margin-end-bot">
      <%= f.submit "Editar Informações", :class => "btn btn-default btn-lg btn-block btn-primary", id: "confirm-ngo" %>
    </div>
  </div>
  <% end %>
</div>
